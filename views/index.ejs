<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <!-- keep a single viewport meta -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Favicon / Logo -->
  <link rel="icon" href="/images/tlogo.jpg" type="image/jpeg">

  <!-- Meta Tags -->
  <meta name="description" content="Portfolio Website of Shaik Mahammad Tarik">
  <meta name="keywords" content="Tarik, Portfolio, Developer, Engineer, Projects, Certifications">
  <meta name="author" content="Shaik Mahammad Tarik">
  <title>Shaik Mahammad Tarik | Portfolio</title>

  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">

  <!-- Your CSS -->
  <link rel="stylesheet" href="/css/style.css">

  <style>
    /* keep your inline additions */
    body { display: flex; flex-direction: column; min-height: 100vh; }
    main { flex: 1; }
    .footer-divider { border-top: 2px solid #17a2b8; margin-top: 40px; }

    .typing {
      display: inline-block;
      border-right: 2px solid #17a2b8;
      white-space: nowrap;
      overflow: hidden;
      animation: blink-cursor 0.7s step-end infinite;
    }
    @keyframes blink-cursor { from { border-color:#17a2b8; } to { border-color:transparent; } }

    /* ✅ make hero responsive without changing your main CSS file */
    @media (max-width: 768px) {
      h1 { font-size: 2rem; text-align: center; }
      .typing { display:block; text-align:center; font-size:1rem; }
    }
  </style>
</head>
<body>
  <!-- ✅ Navbar (from partial) -->
<%- include('partials/navbar') %>


  <!-- ✅ Main Content Wrapper -->
  <main class="mt-5 pt-4 pt-lg-5">
    <!-- ✅ Hero Section -->
    <div class="container">
      <div class="row align-items-center">
        <!-- Intro Text -->
        <div class="col-md-7 text-center text-md-start mb-4 mb-md-0">
          <h1 class="text-info fw-bold mb-3">
            Hi, I'm <span class="blink">Shaik Mahammad Tarik</span>
          </h1>
          <!-- Typing Effect Text -->
          <p class="typing"></p>
        </div>

        <!-- Profile Image -->
        <div class="col-md-5 text-center text-md-end">
          <!-- keep your class for glow, add img-fluid for responsiveness -->
          <img src="/images/Profile.jpg" alt="Profile" class="profile-pic img-fluid">
        </div>
      </div>
    </div>

    <!-- ✅ Extra margin section -->
    <div class="container text-center mt-5 pt-5">
      <h2 class="text-info">Welcome to My Portfolio</h2>
      <p class="lead text-light">Showcasing my journey, skills, projects, and achievements.</p>
    </div>
  </main>

  <!-- Divider before footer -->
  <div class="footer-divider"></div>

  <!-- ✅ Footer -->
  <%- include("partials/footer") %>

  <!-- ✅ JS (order matters: bundle includes Popper for navbar) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/script.js"></script>
</body>
</html>
